import Item from "./Item";

const SideMenu = () => {
  return (
    <div className="side-menu">
      123
      <Item />
    </div>
  );
};

export default SideMenu;

{
  /* <div className="side-menu">
      {itemData.map((item) => (
        <item />
      ))}
      <item />
    </div> */
}
